<div class="dialoguebuilder-editor-wrapper" id="dialoguebuilder-editor-{{cmid}}">
    <div class="row">
        <!-- Sidebar for Characters -->
        <div class="col-md-4">
            <div class="card mb-3">
                <div class="card-header">
                    <h4 class="h5 mb-0">Personagens</h4>
                </div>
                <div class="card-body">
                    <ul id="db-character-list" class="list-group mb-3">
                        <!-- Characters injected by JS -->
                    </ul>
                    <button id="db-add-char-btn" class="btn btn-secondary w-100">
                        <i class="fa fa-user-plus" aria-hidden="true"></i> Adicionar Personagem
                    </button>
                </div>
            </div>
        </div>

        <!-- Main pane for Dialogue -->
        <div class="col-md-8">
            <div class="card mb-3">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4 class="h5 mb-0">Roteiro de Falas</h4>
                    <button id="db-add-line-btn" class="btn btn-primary btn-sm" disabled>
                        <i class="fa fa-comment-plus" aria-hidden="true"></i> + Adicionar Fala
                    </button>
                </div>
                <div class="card-body" id="db-dialogue-lines">
                    <!-- Dialogue lines injected by JS -->
                    <div class="alert alert-info text-center" id="db-empty-lines-msg">
                        Adicione personagens primeiro para poder criar as falas do roteiro.
                    </div>
                </div>
            </div>

            <!-- Submission Form -->
            <form action="edit.php" method="POST" id="db-submit-form" class="mt-3">
                <input type="hidden" name="id" value="{{cmid}}">
                <input type="hidden" name="action" value="save">
                <input type="hidden" name="sesskey" value="{{sesskey}}">
                <input type="hidden" name="dialoguedata" id="db-dialoguedata">
                <div class="d-flex justify-content-end gap-2">
                    <a href="view.php?id={{cmid}}" class="btn btn-outline-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-success">
                        <i class="fa fa-save" aria-hidden="true"></i> Salvar Roteiro
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
